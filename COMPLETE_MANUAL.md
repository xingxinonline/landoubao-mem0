# 🤖 大模型个人助理 - 完整使用手册

## 📖 目录

1. [系统概述](#系统概述)
2. [快速开始](#快速开始)
3. [安装指南](#安装指南)
4. [使用方式](#使用方式)
5. [功能详解](#功能详解)
6. [API文档](#api文档)
7. [常见问题](#常见问题)
8. [高级配置](#高级配置)

---

## 系统概述

### 什么是个人助理？

这是一个基于Zhipu AI GLM-4大模型的智能个人助理系统，具有以下特点：

- **自然对话**: 支持流畅的中文、英文等多语言对话
- **记忆管理**: 通过MCP Server自动保存和检索用户信息
- **上下文理解**: 在对话中自动融合历史记忆，提供个性化服务
- **多种界面**: CLI命令行、Web浏览器、API接口三种使用方式
- **灵活部署**: 支持本地运行和Docker容器化

### 系统架构

```
用户界面层
├─ CLI (命令行交互)
├─ Web (浏览器界面)
└─ API (HTTP接口)
    │
核心助理层
├─ 对话引擎 (PersonalAssistant)
└─ 记忆交互 (MCPServerClient)
    │
后端服务层
├─ LLM服务 (Zhipu GLM-4)
├─ MCP记忆服务 (HTTP SSE)
└─ 向量存储 (Qdrant)
    │
数据存储层
├─ 用户对话历史
├─ 个人记忆库
└─ 会话管理
```

---

## 快速开始

### 1分钟快速启动

```bash
# 第1步: 设置API密钥
$env:ZHIPU_API_KEY = "your_api_key_here"

# 第2步: 启动系统
cd d:\landoubao-mem0
python start_assistant.py

# 第3步: 选择使用方式
# - 打开浏览器访问: http://localhost:8002/static/index.html
# - 或运行CLI: python app/personal_assistant.py
```

---

## 安装指南

### 前置要求

- Python 3.8+
- Zhipu API Key (从 https://open.bigmodel.cn 获取)
- 互联网连接

### 详细安装步骤

#### 第1步: 克隆或下载项目

```bash
cd d:\landoubao-mem0
```

#### 第2步: 安装依赖

```bash
# 使用pip
pip install openai fastapi uvicorn requests pydantic

# 或使用uv (更快)
uv pip install openai fastapi uvicorn requests pydantic
```

#### 第3步: 配置环境变量

**Windows PowerShell:**
```powershell
# 设置API密钥
$env:ZHIPU_API_KEY = "your_zhipu_api_key"

# 其他可选配置
$env:MODELARK_API_KEY = "your_modelark_key"
$env:QDRANT_HOST = "115.190.24.157"
$env:QDRANT_PORT = "6333"
$env:MCP_SERVER_URL = "http://localhost:8001"
```

**或创建 .env 文件:**
```env
ZHIPU_API_KEY=your_zhipu_api_key
MODELARK_API_KEY=your_modelark_key
QDRANT_HOST=115.190.24.157
QDRANT_PORT=6333
LLM_MODEL=glm-4-flash-250414
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
MCP_SERVER_URL=http://localhost:8001
```

#### 第4步: 验证安装

```bash
# 测试API连接
python tests/test_personal_assistant.py
```

---

## 使用方式

### 方式1: CLI 命令行交互 (推荐新手)

```bash
python app/personal_assistant.py
```

**交互界面:**
```
============================================================
🤖 个人助理交互模式
============================================================
命令列表:
  /help      - 显示帮助信息
  /memories  - 显示所有记忆
  /search    - 搜索记忆
  /stats     - 显示记忆统计
  /save      - 下次对话时保存到记忆
  /clear     - 清空当前对话
  /exit      - 退出
============================================================

👤 你: 你好，我叫张三
🤖 助理: 很高兴认识你，张三！...

👤 你: /save
💾 自动保存: 开启

👤 你: /memories
📚 您的记忆:
  1. 名字是张三
  2. ...
```

**常用命令:**

| 命令             | 说明                 | 例子               |
| ---------------- | -------------------- | ------------------ |
| 正常对话         | 输入任何文本进行对话 | `我想了解Python`   |
| `/help`          | 显示帮助             | `/help`            |
| `/save`          | 启用/禁用自动保存    | `/save`            |
| `/memories`      | 显示所有记忆         | `/memories`        |
| `/search 关键词` | 搜索记忆             | `/search 工作经验` |
| `/stats`         | 显示统计信息         | `/stats`           |
| `/clear`         | 清空对话历史         | `/clear`           |
| `/exit`          | 退出程序             | `/exit`            |

### 方式2: Web 浏览器界面 (推荐日常使用)

```bash
python start_assistant.py
```

然后打开浏览器访问: **http://localhost:8002/static/index.html**

**Web界面功能:**
- 💬 实时对话
- 💾 自动保存记忆
- 📚 查看历史记忆
- 📊 统计信息显示
- 👤 多用户支持

### 方式3: API 接口 (推荐集成开发)

#### 3.1 启动API服务

```bash
python app/personal_assistant_web.py
```

#### 3.2 API调用示例

**创建用户会话:**
```bash
curl -X POST http://localhost:8002/api/session
# 返回: {"user_id": "xxx", "created_at": "..."}
```

**进行对话:**
```bash
curl -X POST http://localhost:8002/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "你好",
    "user_id": "your_user_id",
    "save_memory": true
  }'
```

**获取记忆:**
```bash
curl "http://localhost:8002/api/memories?user_id=your_user_id&limit=10"
```

**搜索记忆:**
```bash
curl "http://localhost:8002/api/search?user_id=your_user_id&query=工作&limit=5"
```

**获取统计:**
```bash
curl "http://localhost:8002/api/stats?user_id=your_user_id"
```

---

## 功能详解

### 功能1: 自然对话

```
👤 用户: 我叫李明，我是产品经理
🤖 助理: 很高兴认识你，李明！作为产品经理...

👤 用户: 我的团队有8个人
🤖 助理: 8人团队是一个很好的规模...

👤 用户: 你能帮我分析一下我们的问题吗？
🤖 助理: 根据你提供的信息，我建议...
(助理自动融合了之前的信息，提供个性化分析)
```

### 功能2: 智能记忆

**自动保存:**
```python
# 对话时启用自动保存
assistant.chat("我喜欢编程", save_memory=True)
```

**手动搜索:**
```python
# 搜索相关记忆
results = assistant.search_memories("编程语言")
for result in results:
    print(result)
```

**查看统计:**
```python
# 获取记忆统计
stats = assistant.get_memory_stats()
print(f"总记忆数: {stats['total_memories']}")
```

### 功能3: 多语言支持

自动检测和处理：
- 🇨🇳 **中文 (Chinese)** - 自动识别，提取中文事实
- 🇺🇸 **英文 (English)** - 自动识别，提取英文事实
- 🇯🇵 **日文 (Japanese)** - 自动识别，提取日文事实
- 🇰🇷 **韩文 (Korean)** - 自动识别，提取韩文事实
- 🇸🇦 **阿拉伯文 (Arabic)** - 自动识别，提取阿拉伯文事实
- 🇷🇺 **俄文 (Russian)** - 自动识别，提取俄文事实
- 🇹🇭 **泰文 (Thai)** - 自动识别，提取泰文事实

```python
# 自动检测语言
assistant.chat("我叫王五，是个工程师", save_memory=True)  # 中文
assistant.chat("My name is John", save_memory=True)  # 英文
```

### 功能4: 上下文感知对话

系统会自动：
1. 加载用户的历史记忆
2. 搜索相关的记忆信息
3. 在系统提示中融合这些上下文
4. 生成更贴切的个性化回答

```
步骤1: 用户说"我是产品经理，管理15人团队"
       → 自动保存到记忆库

步骤2: 用户后来问"如何提高团队协作？"
       → 系统搜索相关记忆
       → 加载"产品经理、15人团队"等信息

步骤3: AI生成回答
       → "根据你的产品管理背景和团队规模，我建议..."
```

---

## API文档

### PersonalAssistant 类

```python
from app.personal_assistant import PersonalAssistant

# 初始化助理
assistant = PersonalAssistant(
    user_id="user_123",  # 可选，自动生成UUID
    model="glm-4-flash-250414",
    api_key="your_api_key"
)
```

#### 主要方法

##### chat(user_input, save_memory=False) -> str
进行一次对话

```python
response = assistant.chat("你好", save_memory=True)
print(response)
```

##### load_memories(limit=10) -> List[Dict]
加载用户的记忆

```python
memories = assistant.load_memories(limit=20)
for mem in memories:
    print(mem)
```

##### search_memories(query) -> List[Dict]
搜索记忆

```python
results = assistant.search_memories("工作经验")
```

##### save_memory(user_msg, assistant_msg) -> bool
保存对话到记忆

```python
success = assistant.save_memory("用户问题", "助理回答")
```

##### get_memory_stats() -> Dict
获取记忆统计

```python
stats = assistant.get_memory_stats()
print(stats)  # {'total_memories': 45, 'user_id': '...', ...}
```

### MCPServerClient 类

```python
from app.personal_assistant import MCPServerClient

client = MCPServerClient(base_url="http://localhost:8001")
```

#### 主要方法

```python
# 添加记忆
result = client.add_memory(
    messages=[{"role": "user", "content": "..."}],
    user_id="user_id",
    metadata={}
)

# 搜索记忆
results = client.search_memory("query", "user_id", limit=5)

# 获取所有记忆
memories = client.get_all_memories("user_id", limit=100)

# 删除记忆
client.delete_memory("memory_id")

# 获取统计
stats = client.get_memory_stats("user_id")

# 检测语言
lang_info = client.detect_language("你好")
```

---

## 常见问题

### Q1: 为什么无法连接MCP Server？

**A:** 检查以下事项：
```bash
# 1. 检查MCP Server是否运行
curl http://localhost:8001/health

# 2. 启动MCP Server
python app/mcp_server_http.py

# 3. 检查防火墙设置
```

### Q2: API密钥有效期是多少？

**A:** 根据Zhipu提供的密钥而定。如果遇到"Invalid API Key"错误：
1. 前往 https://open.bigmodel.cn 验证密钥
2. 确保密钥没有过期
3. 重新设置环境变量

### Q3: 如何清除用户的所有记忆？

**A:** 
```python
# 方式1: 通过助理实例
assistant.mcp_client.delete_all_memories(assistant.user_id)

# 方式2: 通过API
DELETE /api/memories?user_id=user_id
```

### Q4: 对话可以支持多长？

**A:** 
- 单条消息限制: 2000 tokens (约1500个中文字)
- 对话历史: 默认保留最近10条消息
- 记忆加载: 默认加载100条记忆

### Q5: 如何自定义AI的性格或专长？

**A:** 修改系统提示词：
```python
assistant.SYSTEM_PROMPT = """
你是一个专业的技术顾问...
(自定义提示词)
"""
```

### Q6: 如何在生产环境部署？

**A:** 参考[高级配置](#高级配置)部分

---

## 高级配置

### 配置1: Docker部署

```dockerfile
# Dockerfile
FROM python:3.11

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "app/personal_assistant_web.py"]
```

```bash
# 构建镜像
docker build -t personal-assistant .

# 运行容器
docker run -p 8002:8002 \
  -e ZHIPU_API_KEY=your_key \
  -e MCP_SERVER_URL=http://mcp-server:8001 \
  personal-assistant
```

### 配置2: 性能优化

```python
# 优化记忆加载
assistant.load_memories(limit=50)  # 减少加载数量

# 优化API超时
mcp_client = MCPServerClient(timeout=60)

# 优化对话历史
# 修改系统代码中的 self.context.messages[-10:]
```

### 配置3: 日志记录

```python
import logging

logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('assistant.log'),
        logging.StreamHandler()
    ]
)

logger = logging.getLogger(__name__)
assistant = PersonalAssistant()
```

### 配置4: 自定义模型

```python
# 使用其他GLM模型
assistant = PersonalAssistant(model="glm-4-plus")

# 使用OpenAI GPT模型 (需要修改配置)
assistant.llm_client = OpenAI(api_key="sk-...")
assistant.model = "gpt-4"
```

### 配置5: 多用户隔离

```python
# 为每个用户创建独立实例
users = {}

def get_assistant(user_id):
    if user_id not in users:
        users[user_id] = PersonalAssistant(user_id=user_id)
    return users[user_id]

# 使用
assistant = get_assistant("user_123")
response = assistant.chat("你好")
```

---

## 技术栈

| 组件       | 技术              | 版本   |
| ---------- | ----------------- | ------ |
| 大模型     | Zhipu GLM-4       | 250414 |
| Web框架    | FastAPI           | 0.100+ |
| API客户端  | OpenAI Python SDK | 1.0+   |
| 向量数据库 | Qdrant            | 2.0+   |
| 记忆模块   | Mem0              | 1.0+   |
| 编程语言   | Python            | 3.8+   |

---

## 运行流程图

```
┌─────────────────────────────────────────────────────────┐
│                  用户输入                                │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  预处理 & 验证输入     │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  加载用户记忆          │
        │  (最近5-10条)          │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  构建上下文感知提示    │
        │  (融合系统提示+记忆)  │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  调用GLM-4大模型       │
        │  生成回答              │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  可选: 保存到记忆      │
        │  (使用事实提取)        │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  返回响应给用户        │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  更新会话历史          │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  显示给用户            │
        └────────────────────────┘
```

---

## 文件结构

```
landoubao-mem0/
├── app/
│   ├── main.py                    # REST API服务器
│   ├── mcp_server_http.py         # MCP Server (HTTP SSE)
│   ├── personal_assistant.py      # 核心助理类 ✨
│   └── personal_assistant_web.py  # Web API服务器
├── tests/
│   └── test_personal_assistant.py # 功能测试脚本
├── static/
│   └── index.html                 # Web前端界面
├── PERSONAL_ASSISTANT_GUIDE.md    # 个人助理指南
├── start_assistant.py             # 一键启动脚本
└── README.md                       # 项目说明
```

---

## 获取帮助

1. **查看日志**: 启用DEBUG模式查看详细日志
2. **运行测试**: `python tests/test_personal_assistant.py`
3. **检查API**: `curl http://localhost:8001/health`
4. **查阅文档**: 阅读本手册和代码注释

---

## 许可证

MIT License

---

## 更新日志

### v1.0.0 (2024年)
- ✅ 初始版本发布
- ✅ 支持CLI、Web、API三种界面
- ✅ 集成MCP Server记忆模块
- ✅ 多语言支持
- ✅ 上下文感知对话
- ✅ 自动语言检测

---

**祝你使用愉快！** 🚀

如有任何问题，欢迎反馈和建议。

